cmd

pip install esphome
(ESPHome 설치)

파이썬 버전 오류가 있을 수 있습니다 파이썬 버전 오류가 나온다면 버전을 낮춰주세요(3.12.n 버전(또는 그 이하))

https://github.com/byeoncoding/Capstone-HW
하드웨어 파일에 heartrate.yaml 파일을 다운받아주세요

heartrate.yaml 파일의 wifi정보를 수정해야함

수정 후 

py -m esphome run heartrate.yaml
(파이썬이 여러버전 있어서 오류나는 경우도 있음. 파이썬 버전을 지정해서 해야함
 ex) py -x.xx -m esphome run heartrate.yaml)

실시간 데이터 수신 명령어 (센서의 해당 ip입력)
curl -N http://xxx.xxx.xxx.xxx/events